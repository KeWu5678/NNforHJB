# Registered model: signed shallow network with profile insertion.
# (= registry.py ALIASES["signed"])
kind: signed
insertion: profile
activation: relu
power: 1.0
loss_weights: [1.0, 1.0]   # [w1, w2] = [value, gradient]; l2 = [1.0, 0.0]
