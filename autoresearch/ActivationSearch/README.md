# Activation Search

This experiment family compares activation functions across datasets.

- `data:VDP/` contains the smooth VDP HJB activation search.
- `data:analytical/` contains the analytic nonsmooth target whose value is
  continuous but whose gradient jumps across a known curve.

Per-seed JSON files, append-only scoreboards, and regenerated plots are local
artifacts unless explicitly promoted to curated summaries.
